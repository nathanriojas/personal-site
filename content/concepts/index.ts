// Barrel for the four fictional concept-site copy modules rendered under
// /websites/examples/*. Each concept is a separate file because the four sites
// deliberately have different structures (a restaurant has a menu, a
// contractor has service areas and reviews) — they are NOT forced into one
// shared schema.
//
// Everything in these files is invented. Addresses, phone numbers, emails,
// license numbers, reviews, and stats are all placeholders and must stay
// obviously fictional (555 numbers, -example.com domains). The concept routes
// are noindex + excluded from the sitemap so they can never be mistaken for
// real businesses.
export { emberRye } from "./ember-rye"
export { cornerstone, type CornerstoneIcon } from "./cornerstone"
export { ashfordVale } from "./ashford-vale"
export { mayaEllison } from "./maya-ellison"
